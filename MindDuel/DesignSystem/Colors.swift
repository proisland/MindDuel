import SwiftUI

extension Color {
    // Backgrounds
    static let mdBg       = Color("bg")
    static let mdBgDeep   = Color("bg-deep")
    static let mdSurface  = Color("surface")
    static let mdSurface2 = Color("surface-2")
    static let mdBorder   = Color("border")
    static let mdBorder2  = Color("border-2")

    // Text
    static let mdText     = Color("text")
    static let mdText2    = Color("text-2")
    static let mdText3    = Color("text-3")
    static let mdText4    = Color("text-4")

    // Accent (Pi-modus / indigo)
    static let mdAccent     = Color("accent")
    static let mdAccentDeep = Color("accent-deep")
    static let mdAccentSoft = Color("accent-soft")

    // Pink (Regning-modus)
    static let mdPink     = Color("pink")
    static let mdPinkDeep = Color("pink-deep")
    static let mdPinkSoft = Color("pink-soft")

    // Semantic
    static let mdGreen     = Color("green")
    static let mdGreenSoft = Color("green-soft")
    static let mdAmber     = Color("amber")
    static let mdAmberSoft = Color("amber-soft")
    static let mdRed       = Color("red")
    static let mdRedSoft   = Color("red-soft")
}

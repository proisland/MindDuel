# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is uninitialized. It currently contains only `README.md` (a single `# MindDuel` heading) and no source code, package manifests, build configuration, tests, or tooling.

There are no build, lint, test, or run commands to document yet. There is no architecture to describe.

When real code lands, replace this file with concrete guidance covering:
- Commands (build, lint, test, run a single test, dev server)
- The chosen stack and project layout conventions
- The big-picture architecture (module boundaries, data flow, key abstractions) that requires reading multiple files to understand

## Branching

Active development branch for the current task: `claude/init-project-8phWJ`. Push work to that branch unless instructed otherwise.
